# 2026-09-03 — Tristan

## What we worked on
- Got local dev running end-to-end: `.env` wired up with `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`, `npm install` run, `supabase-test.js` renamed to `supabase-test.cjs` (repo is ESM, script uses `require`)
- Audited Home, GymMap, Leaderboard, and Profile against the actual Supabase schema and the Pencil design file

## Why
- Needed a working local setup before making further changes
- Found the design/schema audit worth doing now: `routes` table columns (`wall`, `tag_color`, `active`) don't match some older field names still referenced in a few components (`color`, `setter`) — `services/supabase.js` already normalizes most of this, but Leaderboard.jsx has one spot (`route.color` instead of `route.tag_color`) that still reads the wrong field, so hold-color dots there render gray instead of the real color
- Also noticed the signed-out "Me" tab links to `/profile` but only `/profile/:id` is a registered route, and `Profile.jsx` doesn't actually read the `:id` param — worth deciding together whether to wire up viewing other users' profiles or simplify the route

## Update — later same day
- Added `ClimbApp.pen` (the Pencil design canvas) to the repo root so Vinny can open the design file directly instead of needing separate access — it's meant to be worked on together
- Confirmed `.env` stays gitignored (never pushed — each of us keeps our own local Supabase keys)
- Pushed: gitignore update, `package-lock.json` from `npm install`, `supabase-test.js` → `.cjs` rename, and `ClimbApp.pen`
- Heads up: repo is currently **public** on GitHub — `ClimbApp.pen` and everything in `team-notes/` is visible to anyone, not just us. Worth deciding together if we want to flip the repo to private.
